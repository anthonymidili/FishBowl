<table class="table table-striped table-bordered">
  <tr>
    <th>Date Taken</th>
    <th>Nitrate</th>
    <th>Nitrite</th>
    <th>Hardness</th>
    <th>Alkalinity</th>
    <th>pH</th>
    <th></th>
  </tr>
  <%= render 'fields', f: f %>
  <% @water_healths.each do |water_health| %>
    <tr>
      <td><%= water_health.created_at.strftime("%b. %d, %Y @ %I:%M%p") %></td>
      <td><%= water_health.nitrate %></td>
      <td><%= water_health.nitrite %></td>
      <td><%= water_health.hardness %></td>
      <td><%= water_health.alkalinity %></td>
      <td><%= water_health.ph %></td>
      <td class="center">
        <%= link_to [@bowl, water_health],
                    confirm: 'Are you sure? This will permanently delete your water health test!',
                    method: :delete do %>
          <i class="icon-remove"></i>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>
