<h2><%= "#{@bowl.name} Fish Bowl #{@bowl.water_type} Notes" %></h2>

<p>
  <%= link_to "To #{@bowl.name} Bowl", bowl_path(@bowl) %> |
  <%= link_to "Back to #{@bowl.water_type} Health", bowl_test_results_path(@bowl) %>
</p>


<%= render 'form' %>

<%= paginate @notes %>

<table class="table table-striped table-bordered">
  <tr>
    <th class="span3">Date Taken</th>
    <th colspan="6"><%= ('Note').pluralize(@notes.count) %></th>
    <th class="span1">Remove</th>
  </tr>
  <%= render @notes %>
</table>

<%= paginate @notes %>

<p>
  <%= link_to "To #{@bowl.name} Bowl", bowl_path(@bowl) %> |
  <%= link_to "Back to #{@bowl.water_type} Health", bowl_test_results_path(@bowl) %>
</p>
